name: 'Install'
description: 'Sets up Node.js and runs install'

runs:
  using: composite
  steps:
    # No `version` input: pnpm/action-setup reads the `packageManager`
    # field from package.json, keeping CI and local installs in sync.
    - uses: pnpm/action-setup@v6

    # No `registry-url`: it writes an .npmrc pointing at NODE_AUTH_TOKEN, and
    # that empty auth entry shadows the OIDC credentials used to publish.
    - name: Setup Node.js
      uses: actions/setup-node@v7
      with:
        node-version-file: '.nvmrc'
        cache: 'pnpm'

    - name: Setup Git User
      shell: bash
      run: |
        git config --global user.email "samuel.billaud1990@gmail.com"
        git config --global user.name "Samuel Billaud"

    - name: Install dependencies
      shell: bash
      run: pnpm install
